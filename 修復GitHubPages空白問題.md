# 修復 GitHub Pages 空白頁面問題

## 🔍 問題原因

GitHub Pages 顯示空白頁面的常見原因：
1. **Base path 設定錯誤** - Vite 需要知道 GitHub Pages 的子路徑
2. **資源路徑錯誤** - JavaScript 和 CSS 檔案路徑不正確
3. **建置輸出問題** - dist 目錄沒有正確建置

## ✅ 已修復的設定

### 1. 更新 vite.config.js
已設定正確的 base path：
```javascript
base: '/cursor-JR/', // GitHub Pages base path
```

### 2. 更新 package.json
已添加部署腳本：
```json
"deploy": "npm run build && gh-pages -d dist"
```

## 📋 修復步驟

### 步驟 1：安裝 gh-pages（如果還沒安裝）

在終端機執行：
```bash
npm install --save-dev gh-pages
```

### 步驟 2：重新建置專案

```bash
npm run build
```

### 步驟 3：部署到 GitHub Pages

```bash
npm run deploy
```

這個指令會：
1. 建置專案（生成 dist 目錄）
2. 將 dist 目錄推送到 gh-pages 分支
3. GitHub Pages 會自動從 gh-pages 分支部署

### 步驟 4：啟用 GitHub Pages

1. **前往儲存庫設定**
   - 訪問：https://github.com/liao09liao-bot/cursor-JR/settings

2. **進入 Pages 設定**
   - 在左側選單點擊 **"Pages"**

3. **設定來源**
   - **Source**: 選擇 **"Deploy from a branch"**
   - **Branch**: 選擇 **"gh-pages"** 分支
   - **Folder**: 選擇 **"/ (root)"**
   - 點擊 **"Save"**

4. **等待部署**
   - GitHub Pages 需要幾分鐘來部署
   - 部署完成後，訪問：https://liao09liao-bot.github.io/cursor-JR/

## 🔄 更新網站

每次修改程式碼後，重新部署：

```bash
npm run deploy
```

## 🧪 本地測試

在部署前，可以先本地測試：

```bash
# 建置專案
npm run build

# 預覽建置結果
npm run preview
```

然後訪問預覽網址（通常是 http://localhost:4173）確認網站正常顯示。

## ✅ 驗證修復

部署完成後，檢查：

1. **網站可以訪問**
   - 訪問：https://liao09liao-bot.github.io/cursor-JR/
   - 應該看到完整的網站內容

2. **所有資源正常載入**
   - 打開瀏覽器開發者工具（F12）
   - 檢查 Console 是否有錯誤
   - 檢查 Network 標籤，確認所有檔案都成功載入

3. **功能正常運作**
   - 測試導航功能
   - 測試表單提交
   - 測試響應式設計

## 🚨 常見問題

### 問題 1：還是顯示空白頁面

**解決方法**：
1. 確認 base path 設定正確：`/cursor-JR/`
2. 清除瀏覽器快取（Ctrl + Shift + Delete）
3. 檢查瀏覽器 Console 是否有錯誤訊息
4. 確認 GitHub Pages 部署狀態為成功

### 問題 2：資源 404 錯誤

**解決方法**：
1. 確認 vite.config.js 中的 base path 設定正確
2. 確認所有資源路徑都是相對路徑
3. 重新執行 `npm run build` 和 `npm run deploy`

### 問題 3：樣式沒有載入

**解決方法**：
1. 檢查 index.css 是否正確引入
2. 確認 Tailwind CSS 配置正確
3. 檢查建置輸出中是否有 CSS 檔案

## 📝 設定說明

### vite.config.js
```javascript
export default defineConfig({
  plugins: [react()],
  base: '/cursor-JR/', // 必須與儲存庫名稱一致
})
```

### package.json
```json
{
  "scripts": {
    "deploy": "npm run build && gh-pages -d dist"
  }
}
```

## 🔗 相關連結

- **GitHub 儲存庫**: https://github.com/liao09liao-bot/cursor-JR
- **GitHub Pages 設定**: https://github.com/liao09liao-bot/cursor-JR/settings/pages
- **網站預覽**: https://liao09liao-bot.github.io/cursor-JR/

## 💡 替代方案

如果 GitHub Pages 還是有問題，建議使用：

1. **Vercel**（推薦）
   - 不需要設定 base path
   - 自動部署
   - 更快的載入速度

2. **Netlify**
   - 簡單設定
   - 自動部署
   - 免費 SSL

---

**執行 `npm run deploy` 後，等待幾分鐘，網站應該就能正常顯示了！**

