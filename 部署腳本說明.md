# 部署腳本說明

## 📋 當前 package.json 腳本設定

### ✅ 正確的 Vite + React 專案腳本

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  }
}
```

## 🔍 說明

### 1. `build` 腳本
```json
"build": "vite build"
```
- **用途**：建置 Vite 專案
- **輸出**：`dist/` 目錄
- **說明**：這是 Vite 專案的正確建置命令，不是 VuePress

### 2. `deploy` 腳本
```json
"deploy": "npm run build && gh-pages -d dist"
```
- **用途**：建置專案並部署到 GitHub Pages
- **步驟**：
  1. 執行 `npm run build` 建置專案
  2. 使用 `gh-pages` 將 `dist` 目錄部署到 GitHub Pages
- **說明**：這是 Vite 專案的正確部署命令

## ⚠️ 注意事項

### VuePress vs Vite 差異

您提供的 VuePress 腳本：
```json
"build": "vuepress build docs",
"deploy": "gh-pages -d docs/.vuepress/dist"
```

**不適用於此專案**，因為：
- ❌ 這是 VuePress 專案的腳本
- ❌ 此專案是 Vite + React，不是 VuePress
- ❌ VuePress 輸出到 `docs/.vuepress/dist`
- ✅ Vite 輸出到 `dist/`

### 正確的設定

對於 **Vite + React** 專案：
- ✅ `build`: `vite build` - 建置到 `dist/`
- ✅ `deploy`: `npm run build && gh-pages -d dist` - 部署 `dist/` 目錄

## 📦 安裝 gh-pages

如果需要部署到 GitHub Pages，請確保已安裝 `gh-pages`：

```bash
npm install --save-dev gh-pages
```

## 🚀 使用方法

### 1. 建置專案
```bash
npm run build
```

### 2. 部署到 GitHub Pages
```bash
npm run deploy
```

或分別執行：
```bash
npm run build
npx gh-pages -d dist
```

## 📁 目錄結構

### Vite 專案結構
```
cursor-JR/
├── dist/              # 建置輸出目錄（部署這個）
│   ├── index.html
│   └── assets/
├── src/               # 源碼目錄
├── vite.config.js     # Vite 配置
└── package.json       # 專案配置
```

### VuePress 專案結構（參考，不適用）
```
vuepress-project/
├── docs/
│   └── .vuepress/
│       └── dist/      # VuePress 輸出目錄
└── package.json
```

## ✅ 確認清單

- [x] `build` 腳本使用 `vite build`
- [x] `deploy` 腳本部署 `dist/` 目錄
- [ ] `gh-pages` 已安裝（執行 `npm install --save-dev gh-pages`）
- [x] `vite.config.js` 已設定正確的 base path

## 🔧 如果需要修改

### 修改建置輸出目錄
在 `vite.config.js` 中：
```javascript
export default defineConfig({
  build: {
    outDir: 'dist', // 可以修改為其他目錄
  },
})
```

### 修改部署目錄
在 `package.json` 中：
```json
"deploy": "npm run build && gh-pages -d dist"
```
將 `dist` 改為您想要的目錄名稱。

## 📝 總結

**當前設定是正確的！**

- ✅ 使用 `vite build` 建置（不是 `vuepress build`）
- ✅ 部署 `dist/` 目錄（不是 `docs/.vuepress/dist`）
- ✅ 適合 Vite + React 專案

**不需要修改，除非您要改用 VuePress（這需要完全重構專案）。**

---

**如果需要部署，請執行：**
```bash
npm install --save-dev gh-pages
npm run deploy
```

