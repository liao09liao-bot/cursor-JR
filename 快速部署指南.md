# 快速部署指南

## 🚀 Vercel 部署（最簡單，推薦）

### 步驟 1：前往 Vercel
訪問：https://vercel.com

### 步驟 2：登入
使用您的 GitHub 帳號登入

### 步驟 3：匯入專案
1. 點擊 **"Add New..."** → **"Project"**
2. 在專案列表中找到 `cursor-JR` 或 `liao09liao-bot/cursor-JR`
3. 點擊 **"Import"**

### 步驟 4：設定專案
Vercel 會自動偵測 Vite 專案，設定如下：
- **Framework Preset**: Vite（自動偵測）
- **Root Directory**: `./`（預設）
- **Build Command**: `npm run build`（自動設定）
- **Output Directory**: `dist`（自動設定）
- **Install Command**: `npm install`（自動設定）

### 步驟 5：部署
1. 點擊 **"Deploy"**
2. 等待建置完成（約 1-2 分鐘）
3. 獲得網站網址（例如：`cursor-jr.vercel.app`）

### 步驟 6：自訂網域（可選）
1. 在 Vercel 專案設定中點擊 **"Domains"**
2. 添加您的自訂網域
3. 按照指示設定 DNS

## 🌐 Netlify 部署

### 步驟 1：前往 Netlify
訪問：https://netlify.com

### 步驟 2：登入
使用您的 GitHub 帳號登入

### 步驟 3：匯入專案
1. 點擊 **"Add new site"** → **"Import an existing project"**
2. 選擇 **"GitHub"**
3. 授權 Netlify 存取您的 GitHub 帳號
4. 選擇 `cursor-JR` 儲存庫

### 步驟 4：設定建置
- **Branch to deploy**: `main`
- **Build command**: `npm run build`
- **Publish directory**: `dist`

### 步驟 5：部署
1. 點擊 **"Deploy site"**
2. 等待建置完成
3. 獲得網站網址（例如：`cursor-jr.netlify.app`）

## 📦 GitHub Pages 部署

### 步驟 1：安裝 gh-pages
```bash
npm install --save-dev gh-pages
```

### 步驟 2：更新 package.json
在 `package.json` 的 `scripts` 中添加：
```json
{
  "scripts": {
    "deploy": "npm run build && gh-pages -d dist"
  }
}
```

### 步驟 3：設定 Vite
在 `vite.config.js` 中添加：
```javascript
export default defineConfig({
  plugins: [react()],
  base: '/cursor-JR/', // 替換為您的儲存庫名稱
})
```

### 步驟 4：部署
```bash
npm run deploy
```

### 步驟 5：啟用 GitHub Pages
1. 前往儲存庫的 **"Settings"**
2. 點擊 **"Pages"**
3. 選擇 **"Source"** 為 `gh-pages` 分支
4. 儲存設定
5. 訪問：`https://liao09liao-bot.github.io/cursor-JR/`

## ✅ 部署後檢查

部署完成後，請確認：

1. **網站可以正常訪問**
   - 打開網站網址
   - 確認所有頁面正常顯示

2. **功能正常運作**
   - 測試導航功能
   - 測試表單提交
   - 測試響應式設計

3. **更新 GitHub 儲存庫**
   - 在儲存庫設定中添加網站連結
   - 更新 README.md 添加網站連結

## 🔄 自動部署

### Vercel 和 Netlify
- ✅ 自動部署：每次推送到 GitHub 都會自動重新部署
- ✅ 預覽部署：Pull Requests 會自動建立預覽版本

### GitHub Pages
- ⚠️ 需要手動執行 `npm run deploy` 來更新網站

## 🌍 自訂網域

### Vercel
1. 在專案設定中點擊 **"Domains"**
2. 輸入您的網域
3. 按照指示設定 DNS 記錄

### Netlify
1. 在網站設定中點擊 **"Domain settings"**
2. 點擊 **"Add custom domain"**
3. 輸入您的網域
4. 按照指示設定 DNS 記錄

## 📊 部署狀態

部署後，您可以：
- 查看部署日誌
- 查看網站分析
- 設定環境變數
- 設定自訂網域

## 🆘 常見問題

### 問題 1：建置失敗
**解決方法**：
- 檢查 `package.json` 中的依賴是否正確
- 確認 `npm install` 可以正常執行
- 檢查建置日誌中的錯誤訊息

### 問題 2：網站無法訪問
**解決方法**：
- 確認部署狀態為成功
- 檢查網址是否正確
- 確認 DNS 設定（如果使用自訂網域）

### 問題 3：資源載入失敗
**解決方法**：
- 檢查 `vite.config.js` 中的 `base` 設定
- 確認所有資源路徑都是相對路徑

---

**推薦使用 Vercel**，因為它最簡單且自動化程度最高！

部署完成後，記得更新 GitHub 儲存庫的網站連結！

